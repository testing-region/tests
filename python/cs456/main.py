# problem 7
def pair_points(x: list[int], y: list[int]) -> tuple[int, int]:
    """List of x and y coordinates of points

    Args:
        x: list[int]: list of x coordinates
        y: list[int]: list of y coordinates

    Returns:
        tuple[int, int]: tuple of two integers that represent
        the pair of points closest to each other
    """
    pass


# problem 8
def find_max_diff(arr: list[int]) -> int:
    """Find the maximum difference between two elements in the array

    Args:
        arr: list[int]: list of integers

    Returns:
        int: maximum difference between two elements in the array
    """
    pass
