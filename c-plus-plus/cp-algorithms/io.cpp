#include <bits/stdc++.h>
using namespace std;


int main() {
    // To make i/o more efficient add the lines below at the beginning of the code
    ios::sync_with_stdio(0);
    cin.tie(0);

    int a, b;
    string x;

    cin >> a >> b >> x;
    cout << a << " " << b << " " << x << "\n";
    // "\n" works faster than endl
}

